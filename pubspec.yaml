name: my_porki
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  excel: ^2.0.1
  permission_handler: ^11.0.1
  open_file: ^3.3.1
  
  # Firebase - VERSIÓN ESTABLE
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0
  cloud_firestore: ^6.0.2
  firebase_messaging: ^16.0.4
  
  # Google Sign In
  google_sign_in: ^7.2.0
  
  # Connectivity
  connectivity_plus: ^7.0.0
  
  # Hive - VERSIONES COMPATIBLES
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # Utilidades
  path_provider: ^2.1.5
  intl: ^0.20.2
  flutter_local_notifications: ^17.1.2
  timezone: ^0.9.0
  crypto: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  
  # GENERADORES - VERSIONES 100% COMPATIBLES
  hive_generator: ^1.1.3
  build_runner: ^2.4.13
  
  # Iconos y splash
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/LogoAlex.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/LogoAlex.png

flutter_native_splash:
  color: "#ffffff"
  image: assets/images/LogoAlex.png
  android: true
  ios: true